---
title: "Testing MDX Rendering in GPU Compare"
description: "A quick test page to verify that next-mdx-remote, gray-matter, and tailwind-typography render beautifully."
date: "2026-06-02"
author: "Antigravity AI"
readTime: "2 min read"
tags: ["Next.js", "MDX", "Typography"]
category: "Meta"
---

# Hello, MDX!

This is a test of the Next.js 14 dynamic MDX rendering engine.

## Code Block Styling Test
Here is some standard Python code for testing syntax highlighting or formatting:

```python
import numpy as np

def test_inference(gpu_model, batch_size):
    print(f"Running inference on {gpu_model} with batch size {batch_size}...")
    latency = np.random.uniform(1.2, 5.8)
    return f"Success! Latency: {latency:.2f}ms"
```

## Checklist / Elements Test
- [x] MDX files are parsed on the server using `gray-matter`.
- [x] Content is rendered using `next-mdx-remote/rsc`.
- [ ] CSS is styled using `@tailwindcss/typography`.

Let's test an inline affiliate link component as well. If it resolves correctly, this text will look standard, but we'll wire dynamic components like custom affiliate alerts later.
